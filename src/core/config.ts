/**
 * Единая точка баланса прототипа. Все «магические числа» боя — здесь,
 * чтобы крутить баланс, не трогая логику.
 */
export const BALANCE = {
  player: {
    /** Стартовое HP Пепельного */
    maxHp: 70,
    /** Энергии за ход */
    energyPerTurn: 3,
    /** Карт в руке в начале хода */
    cardsPerTurn: 5,
    // Стартовые колоды классов — в src/data/classes.ts
  },
  combat: {
    /** Множитель урона при Ярости атакующего */
    furyDamageMult: 1.5,
    /** Множитель блока при Ярости */
    furyBlockMult: 0.5,
    /** Множитель получаемого урона при пробитой броне */
    vulnerableDamageMult: 1.5,
  },
  /** Экономика и правила хаба */
  hub: {
    /** Минимальный размер боевой колоды (цель по ТЗ — 15, поднят когда карт станет больше) */
    minDeck: 10,
    maxDeck: 30,
    /** Кузня: стоимость уровня улучшения = base * (текущий уровень + 1) */
    upgradeBaseCost: 100,
    /** Горнило: стоимость создания карты */
    craftSoulCost: 150,
    craftEssenceCost: 1,
  },
  /** Прогрессия глубины похода */
  progression: {
    /** +12% HP врага за глубину свыше первой */
    hpGrowthPerDepth: 0.12,
    /** +25% душ за глубину */
    soulsGrowthPerDepth: 0.25,
    /** +4% шанса угля за глубину (потолок 60%) */
    essenceGrowthPerDepth: 0.04,
    /** Плоский +1 урон врагу за каждые 2 глубины */
    dmgBonusPerDepth: 0.5,
    /** Этаж босса: после стольких выборов узлов (карта генерится целиком) */
    bossEveryDepth: 15,
    /** Этаж гарантированной элиты (мини-босс середины похода) */
    eliteFloor: 8,
  },
  /** Экономика похода (граф: 1 узел на этаж — доход подогнан под 15 этажей) */
  trial: {
    /** Души за обычный бой: base + depth × perDepth */
    battleBaseSouls: 50,
    battleSoulsPerDepth: 10,
    /** Множитель за элитный узел */
    eliteSoulsMult: 1.5,
    /** Души за боссовый узел */
    bossSouls: 300,
    /** Души с события «Находка» (вариант денег) */
    eventSouls: 60,
    /** Торговец: base + depth × perDepth */
    shopBaseCost: 40,
    shopCostPerDepth: 6,
  },
} as const;
